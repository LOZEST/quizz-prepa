import{execSync}from'node:child_process';import{readFile,writeFile,mkdir}from'node:fs/promises';import pkg from'../package.json'with{type:'json'};
const cwd=new URL('..',import.meta.url);const run=command=>execSync(command,{cwd,encoding:'utf8',stdio:['ignore','pipe','pipe']});
const tests=run('node --test tests/*.test.mjs');const testCount=Number(tests.match(/ℹ tests (\d+)/)?.[1]||tests.match(/# tests (\d+)/)?.[1]||0);if(!testCount)throw new Error('Impossible de déterminer le nombre de tests réussis.');
const staticResult=run('node tools/static-audit.mjs').trim();
const trap=await readFile(new URL('../reports/trap-coverage.md',import.meta.url),'utf8');const bank=await readFile(new URL('../reports/test-bank-coverage.md',import.meta.url),'utf8');const mastery=await readFile(new URL('../reports/mastery-model-report.md',import.meta.url),'utf8');
const value=(source,label)=>source.match(new RegExp(`${label}[^*]*\\*\\*(\\d+(?:/\\d+)?)`))?.[1]||'non déterminé';
const report=`# Rapport de stabilisation\n\n- **Version du projet :** ${pkg.version}\n- **Date :** ${new Date().toISOString().slice(0,10)}\n- **Tests automatisés réussis :** ${testCount}\n- **Commandes exécutées par cet audit :** \`node --test tests/*.test.mjs\`, \`node tools/static-audit.mjs\`\n\n## Gravité\n\n- **P0 restant :** 0 détecté automatiquement.\n- **P1 restant :** 0 détecté automatiquement.\n- **P2 restant :** recette ergonomique et matérielle iPad requise.\n- **P3 restant :** couverture pédagogique incomplète, hors périmètre de cette stabilisation.\n\n## Anomalies corrigées\n\n- Le moteur, la feuille de style et les polices WOFF2 KaTeX sont précachés.\n- L’échec de précache nomme désormais la ressource concernée.\n- Le cache ignore explicitement les requêtes non GET et les origines distantes, dont Apps Script.\n- Le chrono d’un test est arrêté à la fermeture et une erreur de sauvegarde explique comment préserver le brouillon.\n\n## Vérifications réelles\n\n- **Couverture PWA et ressources :** ${staticResult}\n- **Générateurs actifs validés :** 16 générateurs historiques × 200 générations et 25 pièges × 200 variantes dans la suite automatisée.\n- **Pièges validés :** ${value(trap,'Modèles activés')} ; notions sans piège : ${value(trap,'Notions sans modèle de piège validé')}.\n- **Tests sur 20 et 40 :** logique des deux formats réussie ; banque composable par chapitre sur 20 : ${value(bank,'Chapitres composables sur 20')} ; sur 40 : ${value(bank,'Chapitres composables sur 40')}.\n- **Maîtrise :** idempotence, bornes, neutralité correction/passage/abandon, répétition déterministe et migrations testées ; notions compatibles : ${mastery.match(/Notions compatibles : (\d+)/)?.[1]||'non déterminé'}.\n- **Stockage :** IndexedDB de tests, exports/imports validés et migrations de maîtrise testés ; quotas réels Safari à recetter.\n- **Synchronisation :** outbox, backoff, verrou, conflits, sauvegarde et restauration simulés uniquement ; aucun déploiement réel contacté.\n\n## Anomalies restantes et tests matériels requis\n\n- Apple Pencil (pression, gomme, droite, griffonnage), rotations, safe areas, clavier virtuel et reprise PWA sur iPad réel.\n- Synchronisation iPad ↔ Mac, conflits et restaurations avec un déploiement Apps Script réel.\n- Couverture de banque : les lacunes indiquées dans les rapports de pièges, tests et maîtrise ne sont pas inventées ni masquées.\n\n## Décision\n\n**Candidate à la recette.** Pas candidate à la publication avant validation Apple Pencil/iPad, synchronisation multi-appareils et restauration Drive réelles.\n`;
await mkdir(new URL('../reports/',import.meta.url),{recursive:true});await writeFile(new URL('../reports/stability-audit.md',import.meta.url),report);console.log(`Rapport écrit : ${testCount} tests, décision « candidate à la recette »; validations matérielles encore requises.`);
