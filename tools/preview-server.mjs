import http from 'node:http';import { createReadStream, statSync } from 'node:fs';import { extname, join, normalize } from 'node:path';
const root=process.cwd(),types={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.json':'application/json','.svg':'image/svg+xml'};
http.createServer((req,res)=>{const pathname=decodeURIComponent(new URL(req.url,'http://localhost').pathname),file=join(root,normalize(pathname==='/'?'/quiz.html':pathname));try{if(!statSync(file).isFile())throw 0;res.setHeader('Content-Type',types[extname(file)]||'application/octet-stream');createReadStream(file).pipe(res)}catch{res.statusCode=404;res.end('Not found')}}).listen(4173,'0.0.0.0',()=>console.log('Preview: http://localhost:4173/quiz.html'));
