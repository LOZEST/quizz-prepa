import{COURSE_MAP,allNotions}from'../course-map.js';import{QuestionCache}from'../question-bank/question-cache.js';import{QuestionRepository}from'../question-bank/question-repository.js';import{QuestionSync}from'../question-bank/question-sync.js';import{operation,enqueue}from'../question-bank/question-outbox.js';import{validateQuestion}from'../question-bank/question-validator.js';import{generateVariants}from'../question-bank/parameterized-generator.js';import{renderSegments,renderSteps}from'../math/render-math.js';import{toast}from'../shell/toast.js';
const TYPES={course:'Question de cours',formula:'Formule',parameterized:'Exercice avec valeurs variables'},LEVELS={1:'Fondamental',2:'Standard',3:'Réflexe prépa',4:'Piège'};let cache,sync,account,currentTab='common',editing=null,archiveTarget=null;
export async function init(context){account=context.account;cache=new QuestionCache();cache.open(account.userId);sync=context.client?new QuestionSync(cache,new QuestionRepository(context.client)):null;tabs();wire();render();if(sync)sync.sync().then(()=>{status('Synchronisé');render()}).catch(()=>status('Hors connexion : la banque locale reste disponible.'));document.addEventListener('quiz-tsi-account-leaving',()=>{sync?.stop();cache.close()},{once:true})}
function tabs(){const rows=[['common','Banque commune'],['mine','Mes questions'],['draft','Brouillons'],...(['admin','owner'].includes(account.role)?[['publish','À publier'],['archives','Archives']]:[])],root=document.querySelector('[data-tabs]');for(const[id,label]of rows){const b=document.createElement('button');b.type='button';b.className='secondary';b.textContent=label;b.onclick=()=>{currentTab=id;render()};root.append(b)}}
function wire(){document.querySelector('[data-create]').onclick=()=>openEditor();document.querySelector('[data-search]').oninput=render;document.querySelector('[data-difficulty]').onchange=render;const d=document.querySelector('[data-confirm]');document.querySelector('[data-confirm-cancel]').onclick=()=>d.close();document.querySelector('[data-confirm-ok]').onclick=()=>{archive(archiveTarget);d.close()}}
function rows(){const all=[...cache.data.common,...cache.data.private,...cache.data.management],mine=q=>q.author_id===account.userId;let out=currentTab==='common'?all.filter(q=>q.scope==='common'&&q.status==='published'):currentTab==='mine'?all.filter(q=>mine(q)&&q.status==='published'):currentTab==='draft'?all.filter(q=>mine(q)&&q.status==='draft'):currentTab==='publish'?all.filter(q=>q.status==='draft'&&q.scope==='common'):all.filter(q=>q.status==='archived');const query=document.querySelector('[data-search]').value.toLowerCase(),difficulty=document.querySelector('[data-difficulty]').value;return out.filter(q=>(!query||[q.title,q.chapter_id,q.notion_id].join(' ').toLowerCase().includes(query))&&(!difficulty||String(q.difficulty)===difficulty)).filter(q=>q.scope==='common'||mine(q))}
function render(){const root=document.querySelector('[data-question-list]'),list=rows();root.replaceChildren();if(!list.length){const e=document.createElement('div');e.className='empty';e.textContent='Aucune question dans cette section.';root.append(e);return}for(const q of list)root.append(card(q))}
function card(q){const a=document.createElement('article');a.className='card question-card-item';const h=document.createElement('header'),title=document.createElement('strong'),type=document.createElement('span');title.textContent=q.title;type.className='badge';type.textContent=TYPES[q.question_type];h.append(title,type);const meta=document.createElement('p');const notion=allNotions().find(n=>n.id===q.notion_id);meta.textContent=`${notion?.chapterLabel||q.chapter_id} · ${notion?.label||q.notion_id} · ${LEVELS[q.difficulty]} · ${q.status==='published'?'Publiée':q.status==='archived'?'Archivée':'Brouillon'} · ${q.scope==='common'?'Commune':'Privée'} · Modifiée ${date(q.updated_at)}`;const actions=document.createElement('div');actions.className='actions-row';actions.append(action('Aperçu',()=>preview(q)));const own=q.author_id===account.userId,manager=['admin','owner'].includes(account.role);if(own||manager)actions.append(action('Modifier',()=>openEditor(q)));actions.append(action('Dupliquer',()=>duplicate(q,'private')));if(manager)actions.append(action('Dupliquer comme brouillon commun',()=>duplicate(q,'common')));if((own||manager)&&q.status==='draft')actions.append(action('Publier',()=>publish(q)));if((own||manager)&&q.status==='published')actions.append(action('Remettre en brouillon',()=>mutate(q,{status:'draft'})));if((own||manager)&&q.status!=='archived')actions.append(action('Archiver',()=>{archiveTarget=q;document.querySelector('[data-confirm]').showModal()}));a.append(h,meta,actions);return a}
function action(label,fn){const b=document.createElement('button');b.className='secondary';b.textContent=label;b.onclick=fn;return b}function date(v){return v?new Intl.DateTimeFormat('fr-FR',{dateStyle:'short'}).format(new Date(v)):'date inconnue'}function status(v){document.querySelector('[data-bank-status]').textContent=v}
function openEditor(question=null){editing=question;document.querySelector('[data-question-list]').hidden=true;const root=document.querySelector('[data-editor]');root.hidden=false;root.replaceChildren();let step=1,type=question?.question_type||'',variables=question?.variable_spec?.variables||[];root.innerHTML=`<p class="wizard-progress" data-progress>Étape 1 sur 4</p><section class="wizard-step" data-step="1"><h2>Quel type de question ?</h2><div class="cards" data-types></div></section><section class="wizard-step" data-step="2" hidden><h2>Classer la question</h2><div class="field-grid"><label>Partie<select name="part"></select></label><label>Chapitre<select name="chapter"></select></label><label>Notion<select name="notion"></select></label><label>Difficulté<select name="difficulty"><option value="1">1 — Fondamental</option><option value="2">2 — Standard</option><option value="3">3 — Réflexe prépa</option><option value="4">4 — Piège</option></select></label><label>Titre interne<input name="title" maxlength="160"></label><label>Catégorie facultative<input name="category"></label></div><p>1 mémorise l’essentiel ; 2 applique le cours ; 3 demande un réflexe de prépa ; 4 travaille un piège.</p></section><section class="wizard-step" data-step="3" hidden><h2>Écrire le contenu</h2><div data-content></div><div class="actions-row"><button type="button" data-add-text>Ajouter du texte</button><button type="button" data-add-math>Ajouter une formule</button><button type="button" data-add-step>Ajouter une étape</button></div><section data-variables hidden><h3>Variables</h3><div data-variable-list></div><button type="button" data-add-variable>Ajouter une variable</button><label>Contraintes facultatives<textarea name="constraints" placeholder="a != 0\na < b\ngcd(a,b) = 1"></textarea></label><button type="button" class="secondary" data-advanced-toggle>Mode avancé</button><div class="advanced" data-advanced hidden><p>Réservé aux utilisateurs expérimentés. La configuration structurée doit rester sûre.</p><label>JSON structuré<textarea name="advancedJson"></textarea></label></div></section></section><section class="wizard-step" data-step="4" hidden><h2>Aperçu et publication</h2><article class="card" data-preview></article><div data-variants></div></section><div class="actions-row"><button type="button" data-back>Retour</button><button type="button" class="primary" data-next>Continuer</button><button type="button" data-draft hidden>Enregistrer comme brouillon</button><button type="button" class="primary" data-private hidden>Publier dans mes questions</button><button type="button" class="primary" data-common hidden>Publier pour tous</button><button type="button" data-cancel>Annuler</button></div>`;
const content=root.querySelector('[data-content]');content.innerHTML=`<label data-prompt-label>Énoncé visible par l’élève<textarea name="prompt" required></textarea></label><label data-hint-label>Indice facultatif<textarea name="hint"></textarea></label><label data-correction-label>Correction détaillée<textarea name="correction" required></textarea></label><label>Concept ou erreur à retenir<textarea name="concept"></textarea></label><label>Formulation propre à retenir<textarea name="oral"></textarea></label>`;for(const[k,v]of Object.entries({title:question?.title||'',difficulty:question?.difficulty||1,prompt:text(question?.prompt_content),hint:text(question?.hint_content),correction:question?.correction_content?.steps?.map(text).join('\n')||'',concept:text(question?.hidden_concept_content),oral:text(question?.oral_formulation_content)}))if(root.querySelector(`[name=${k}]`))root.querySelector(`[name=${k}]`).value=v;
const part=root.querySelector('[name=part]'),chapter=root.querySelector('[name=chapter]'),notion=root.querySelector('[name=notion]');fill(part,COURSE_MAP);part.onchange=()=>{fill(chapter,COURSE_MAP.find(p=>p.id===part.value)?.chapters||[]);chapter.dispatchEvent(new Event('change'))};chapter.onchange=()=>fill(notion,COURSE_MAP.flatMap(p=>p.chapters).find(c=>c.id===chapter.value)?.notions||[]);part.value=question?.part_id||COURSE_MAP[0].id;part.dispatchEvent(new Event('change'));chapter.value=question?.chapter_id||chapter.value;chapter.dispatchEvent(new Event('change'));notion.value=question?.notion_id||notion.value;
for(const[v,label,desc]of[['course','Question de cours','Faire réciter une définition, une propriété ou un théorème.'],['formula','Formule','Faire retrouver une formule, ses hypothèses ou son domaine de validité.'],['parameterized','Exercice avec valeurs variables','Créer automatiquement plusieurs variantes d’un même exercice.']]){const b=action(label,()=>{type=v;if(v==='formula'){root.querySelector('[data-prompt-label]').firstChild.textContent='Nom ou question sur la formule';root.querySelector('[data-hint-label]').firstChild.textContent='Hypothèses et domaine';root.querySelector('[data-correction-label]').firstChild.textContent='Formule attendue et explication ou démonstration'}go(2)});b.classList.add('card','action-card');const p=document.createElement('span');p.textContent=desc;b.append(p);root.querySelector('[data-types]').append(b)}
function go(n){step=n;root.querySelectorAll('[data-step]').forEach(x=>x.hidden=Number(x.dataset.step)!==step);root.querySelector('[data-progress]').textContent=`Étape ${step} sur 4`;root.querySelector('[data-back]').hidden=step===1;root.querySelector('[data-next]').hidden=step===4;for(const x of ['draft','private','common'])root.querySelector(`[data-${x}]`).hidden=step!==4||(x==='common'&&!['admin','owner'].includes(account.role));root.querySelector('[data-variables]').hidden=type!=='parameterized';if(step===4)showPreview(root,type,variables)}
root.querySelector('[data-next]').onclick=()=>go(Math.min(4,step+1));root.querySelector('[data-back]').onclick=()=>go(Math.max(1,step-1));root.querySelector('[data-cancel]').onclick=closeEditor;root.querySelector('[data-add-text]').onclick=()=>root.querySelector('[name=prompt]').value+='\nNouveau bloc de texte';root.querySelector('[data-add-math]').onclick=()=>root.querySelector('[name=prompt]').value+='\n\\( formule \\)';root.querySelector('[data-add-step]').onclick=()=>root.querySelector('[name=correction]').value+='\nNouvelle étape : ';
function variable(){const name=String.fromCharCode(97+variables.length);variables.push({name,domain:'Z',sampling:{mode:'integer',minimum:1,maximum:10}});renderVariables(root,variables)}root.querySelector('[data-add-variable]').onclick=variable;root.querySelector('[data-advanced-toggle]').onclick=()=>{const a=root.querySelector('[data-advanced]');a.hidden=!a.hidden};if(!variables.length&&type==='parameterized')variable();else renderVariables(root,variables);root.querySelector('[data-draft]').onclick=()=>save(root,type,variables,'draft','private');root.querySelector('[data-private]').onclick=()=>save(root,type,variables,'published','private');root.querySelector('[data-common]').onclick=()=>save(root,type,variables,'published','common');if(type)go(2)}
function renderVariables(root,variables){const list=root.querySelector('[data-variable-list]');list.replaceChildren(...variables.map((v,i)=>{const box=document.createElement('fieldset');box.innerHTML=`<legend>Variable ${v.name}</legend><label>Nom<input data-v="name" value="${v.name}"></label><label>Type<select data-v="type"><option value="integer">Entier</option><option value="natural">Entier naturel</option><option value="nonzero">Entier non nul</option><option value="decimal">Décimal</option><option value="list">Valeur dans une liste</option></select></label><label>Minimum<input data-v="minimum" type="number" value="${v.sampling?.minimum??1}"></label><label>Maximum<input data-v="maximum" type="number" value="${v.sampling?.maximum??10}"></label><label>Valeurs interdites<input data-v="excluded"></label><button type="button">Supprimer</button>`;box.querySelectorAll('[data-v]').forEach(x=>x.oninput=()=>{if(x.dataset.v==='name')v.name=x.value;else if(['minimum','maximum'].includes(x.dataset.v))v.sampling[x.dataset.v]=Number(x.value)});box.querySelector('button').onclick=()=>{variables.splice(i,1);renderVariables(root,variables)};return box}))}
function payload(root,type,variables,statusValue,scope){const n=allNotions().find(x=>x.id===root.querySelector('[name=notion]').value),seg=v=>({segments:[{type:'text',value:String(v||'')}]});let spec=null;if(type==='parameterized'){const constraints=root.querySelector('[name=constraints]').value.split('\n').map(x=>x.trim()).filter(Boolean);spec={variables:variables.map(v=>({...v,domain:v.domain||'Z',sampling:v.sampling||{mode:'integer',minimum:1,maximum:10}})),constraints,promptTemplate:root.querySelector('[name=prompt]').value,hintTemplate:root.querySelector('[name=hint]').value,correctionTemplates:root.querySelector('[name=correction]').value.split('\n').filter(Boolean)}}const now=new Date().toISOString();return{id:editing?.id||crypto.randomUUID(),author_id:editing?.author_id||account.userId,scope,status:statusValue,question_type:type,title:root.querySelector('[name=title]').value,part_id:n?.partId,chapter_id:n?.chapterId,notion_id:n?.id,difficulty:Number(root.querySelector('[name=difficulty]').value),prompt_content:seg(root.querySelector('[name=prompt]').value),hint_content:seg(root.querySelector('[name=hint]').value),correction_content:{steps:root.querySelector('[name=correction]').value.split('\n').filter(Boolean).map(seg)},hidden_concept_content:seg(root.querySelector('[name=concept]').value),oral_formulation_content:seg(root.querySelector('[name=oral]').value),variable_spec:spec,tags:[],version:editing?.version||1,created_at:editing?.created_at||now,updated_at:now}}
function showPreview(root,type,variables){const q=payload(root,type,variables,'draft','private'),p=root.querySelector('[data-preview]');p.replaceChildren();const meta=document.createElement('p'),statement=document.createElement('div'),hint=document.createElement('div'),correction=document.createElement('div');meta.textContent=`${q.chapter_id} · ${q.notion_id} · ${LEVELS[q.difficulty]}`;renderSegments(statement,q.prompt_content);renderSegments(hint,q.hint_content);renderSteps(correction,q.correction_content);p.append(meta,statement,hint,correction);const vr=root.querySelector('[data-variants]');vr.replaceChildren();if(type==='parameterized')try{const variants=generateVariants(q.variable_spec,10);const h=document.createElement('h3');h.textContent='10 variantes contrôlées';vr.append(h,...variants.map(v=>{const x=document.createElement('p');x.textContent=Object.entries(v.values).map(([k,val])=>`${k}=${val.numerator??val}`).join(' · ');return x}))}catch{const e=document.createElement('p');e.textContent='Les contraintes ne permettent pas de produire 10 variantes distinctes. La publication est bloquée.';e.setAttribute('role','alert');vr.append(e)}}
function save(root,type,variables,statusValue,scope){const q=payload(root,type,variables,statusValue,scope),check=validateQuestion(q,{publishing:statusValue==='published'});if(!check.valid){toast(type==='parameterized'?'La configuration des variables n’est pas valide. Vérifie les valeurs indiquées dans « Variables ».':'Certains champs obligatoires sont incomplets.',{type:'error'});return}const target=q.scope==='common'?(q.status==='published'?cache.data.common:cache.data.management):cache.data.private;const old=[...cache.data.common,...cache.data.management,...cache.data.private].find(x=>x.id===q.id);for(const list of[cache.data.common,cache.data.management,cache.data.private]){const i=list.findIndex(x=>x.id===q.id);if(i>=0)list.splice(i,1)}target.push(q);enqueue(cache,operation(q.id,old?'update':'create',q,old?.version??null));cache.save();toast('Enregistré sur cet appareil. En attente de synchronisation.',{type:'success'});sync?.sync().then(()=>toast('Synchronisé.',{type:'success'})).catch(()=>toast('La synchronisation reprendra dès que possible.'));closeEditor()}
function closeEditor(){document.querySelector('[data-editor]').hidden=true;document.querySelector('[data-question-list]').hidden=false;editing=null;render()}
function preview(q){openEditor(q);const root=document.querySelector('[data-editor]');root.querySelector('[data-progress]').textContent='Aperçu de la question';root.querySelectorAll('[data-step]').forEach(x=>x.hidden=x.dataset.step!=='4')}
function duplicate(q,scope){editing=null;const copy={...structuredClone(q),id:crypto.randomUUID(),author_id:account.userId,scope,status:'draft',version:1,title:`Copie de ${q.title}`,created_at:new Date().toISOString(),updated_at:new Date().toISOString()};cache.data[scope==='common'?'management':'private'].push(copy);enqueue(cache,operation(copy.id,'create',copy));cache.save();toast('La copie a été créée comme brouillon.',{type:'success'});render()}
function mutate(q,patch,type='update'){const next={...q,...patch,updated_at:new Date().toISOString()};for(const list of[cache.data.common,cache.data.private,cache.data.management]){const i=list.findIndex(x=>x.id===q.id);if(i>=0)list[i]=next}enqueue(cache,operation(q.id,type,next,q.version));cache.save();render();sync?.sync()}
function publish(q){if(q.scope==='common'&&!['admin','owner'].includes(account.role)){toast('Ton compte n’a pas l’autorisation de publier dans la banque commune.',{type:'error'});return}const check=validateQuestion({...q,status:'published'},{publishing:true});if(!check.valid){toast('La question doit être complétée avant publication.',{type:'error'});return}mutate(q,{status:'published'})}function archive(q){mutate(q,{status:'archived'},'archive');toast('Question archivée.',{type:'success'})}
function fill(select,rows){select.replaceChildren(...rows.map(x=>{const o=document.createElement('option');o.value=x.id;o.textContent=x.label;return o}))}function text(c){return c?.segments?.map(s=>s.value).join('\n')||''}
