export const MASTERY_SCHEMA_VERSION=2;
export function migrateLegacyState(input,{now=new Date(),deviceId='local-device'}={}){const legacy=input&&typeof input==='object'?structuredClone(input):{};if(legacy.version>=2&&Array.isArray(legacy.masteryEvents))return{state:legacy,migrated:false,backup:null};const backup=structuredClone(legacy),events=[];for(const [notionId,value]of Object.entries(legacy.notions||{})){if(!value||typeof value!=='object')continue;const mastery=Number(value.mastery);if(!Number.isFinite(mastery))continue;const at=new Date(Number(value.lastSeenAt)||Date.parse(value.lastSeen)||now).toISOString();events.push({id:`migration:v1:${notionId}`,version:1,eventType:'mastery-migration',occurredAt:at,recordedAt:new Date(now).toISOString(),partId:null,chapterId:null,notionId,source:{type:'legacy-summary',sourceId:'quiz-tsi-state-v1',sourceVersion:1,variantSignature:`legacy:${notionId}`},evidence:{legacyMastery:Math.max(0,Math.min(100,mastery)),legacyStreak:Number.isFinite(value.streak)?value.streak:null,legacyNextReview:value.nextReview||value.dueAt||null,estimated:true},sessionId:null,testId:null,supersedesEventId:null,deletedAt:null,deviceId})}return{migrated:true,backup,state:{...legacy,version:MASTERY_SCHEMA_VERSION,masteryEvents:events,masteryStates:{},reviewQueue:[],migration:{id:'legacy-v1-to-events-v2',completedAt:new Date(now).toISOString()}}}}
