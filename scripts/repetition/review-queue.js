import{reviewPriority}from'./review-priority.js';
export function buildReviewQueue(states,notions,{now=Date.now(),limit=10,mode='daily'}={}){const candidates=notions.map(n=>({notion:n,state:states[n.id],priority:reviewPriority(states[n.id],{now})})).filter(x=>mode==='new'?x.priority.category==='new':mode==='consolidation'?['fragile','progressing','needs-review'].includes(x.state?.status)||x.state?.confidenceScore<50:mode==='reflex'?x.state?.levelMastery?.[1]>=60:mode==='free'||mode==='mix'||['urgent','due'].includes(x.priority.category));candidates.sort((a,b)=>b.priority.score-a.priority.score||a.notion.id.localeCompare(b.notion.id));const selected=[];for(const item of candidates){if(selected.length>=limit)break;const previous=selected.at(-1);if(previous?.notion.chapterId===item.notion.chapterId){const alternative=candidates.find(x=>!selected.includes(x)&&x.notion.chapterId!==item.notion.chapterId);if(alternative&&!selected.includes(alternative)){selected.push(alternative);continue}}selected.push(item)}return selected}
