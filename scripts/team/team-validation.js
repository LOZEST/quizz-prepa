export function validateInvitation({email,displayName,role}){const cleanEmail=String(email||'').trim().toLowerCase(),name=String(displayName||'').trim();if(cleanEmail.length>254||!/^\S+@\S+\.\S+$/.test(cleanEmail))return{error:'Saisissez une adresse e-mail valide.'};if(name.length>80||/[<>\u0000-\u001f]/.test(name))return{error:'Le nom affiché est invalide.'};if(!['user','admin'].includes(role))return{error:'Choisissez un niveau d’autorisation.'};return{value:{email:cleanEmail,displayName:name,role}}}
