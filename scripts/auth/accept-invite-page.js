import{getSupabaseClient}from'./supabase-client.js';import{recoverInviteSession}from'./auth-callback.js';import{validatePassword}from'./password-validation.js';
const status=document.querySelector('[data-invite-status]'),form=document.querySelector('[data-password-form]'),errorNode=document.querySelector('[data-error]');let client;
async function boot(){try{client=await getSupabaseClient();await recoverInviteSession(client);status.textContent='Votre invitation est acceptée. Choisissez maintenant votre mot de passe.';form.hidden=false}catch{status.innerHTML='<strong>Ce lien d’invitation n’est plus valide.</strong><br>Contactez le propriétaire pour recevoir une nouvelle invitation.'}}
form.addEventListener('submit',async event=>{event.preventDefault();const password=form.password.value,error=validatePassword(password,form.confirmation.value);if(error){errorNode.textContent=error;return}const button=form.querySelector('button[type=submit]');button.disabled=true;const{error:updateError}=await client.auth.updateUser({password});if(updateError){errorNode.textContent='Le mot de passe n’a pas pu être enregistré. Demandez une nouvelle invitation.';button.disabled=false;return}form.hidden=true;status.textContent='Votre compte est activé. Redirection vers Quiz TSI…';setTimeout(()=>location.replace('./index.html'),1200)});document.querySelector('[data-toggle-password]').onclick=()=>{const visible=form.password.type==='text';form.password.type=form.confirmation.type=visible?'password':'text';document.querySelector('[data-toggle-password]').textContent=visible?'Afficher les mots de passe':'Masquer les mots de passe'};boot();
