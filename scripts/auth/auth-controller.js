import{getSupabaseClient}from'./supabase-client.js';import{AuthService}from'./auth-service.js';import{AuthView}from'./auth-view.js';import{rememberAccount,readRememberedAccount,setActiveUser,clearRuntimeSession}from'./session-state.js';import{activateWorkspace,deactivateWorkspace}from'../workspace/user-workspace.js';import{hasLegacyData,migrationStatus,migrateLegacy}from'../workspace/legacy-migration.js';
const view=new AuthView();let service,currentUserId,appLoaded=false;
function migrationChoice(workspace){if(!hasLegacyData()||migrationStatus(workspace).decision!=='pending')return;const dialog=document.createElement('dialog');dialog.className='sync-dialog';dialog.innerHTML='<h2>Données locales antérieures</h2><p>Des données locales antérieures ont été trouvées. Voulez-vous les rattacher à ce compte ?</p><div class="dialog-actions"><button value="import" class="primary">Importer dans ce compte</button><button value="decline">Continuer sans importer</button><button value="later">Décider plus tard</button></div>';document.body.append(dialog);dialog.querySelectorAll('button').forEach(button=>button.onclick=()=>{migrateLegacy(workspace,button.value);dialog.close();dialog.remove()});dialog.showModal()}
async function enter(session,{offline=false}={}){const user=session?.user||session;if(!user?.id)return;const changed=currentUserId&&currentUserId!==user.id;if(changed)location.reload();currentUserId=user.id;setActiveUser(user.id);const workspace=activateWorkspace(user.id);let profile={role:user.role||'user',display_name:user.displayName||''};if(!offline)profile=await service.profile(user.id);const account={userId:user.id,email:user.email,displayName:profile.display_name,role:profile.role};rememberAccount(account);view.showApp(account,{offline});migrationChoice(workspace);if(!appLoaded){appLoaded=true;await import('../session/session-dom.js');await import('../app.js')}if(!offline){const{startProgressSync}=await import('../progress-sync/progress-sync.js');startProgressSync({userId:user.id,supabase:service.client})}document.getElementById('logoutButton').onclick=logout}
async function logout(){document.dispatchEvent(new CustomEvent('quiz-tsi-account-leaving'));currentUserId=null;deactivateWorkspace();clearRuntimeSession();await service?.signOut();location.reload()}
view.onLogin(async(email,password)=>{view.loading(true);view.showError();try{await enter(await service.signIn(email,password))}catch(error){view.showError(error.message)}finally{view.loading(false)}});
(async()=>{try{service=new AuthService(await getSupabaseClient());service.onChange(session=>{if(!session&&currentUserId)logout();else if(session)enter(session).catch(()=>view.showLogin())});const session=await service.restore();if(session)await enter(session);else view.showLogin()}catch(error){const remembered=readRememberedAccount();if(!navigator.onLine&&remembered)await enter(remembered,{offline:true});else view.showError('Service de connexion indisponible. Vérifiez la configuration et la connexion réseau.')}})();
