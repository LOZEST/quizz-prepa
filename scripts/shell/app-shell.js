import{renderNavigation}from'./navigation.js';
export function mountShell({account,activePage,hasSession,onLogout}){const shell=document.querySelector('[data-app-shell]'),nav=document.querySelector('[data-sidebar]');renderNavigation(nav,{account,activePage,hasSession,onLogout});document.querySelector('[data-menu-toggle]').onclick=()=>nav.classList.toggle('open');document.querySelector('[data-nav-close]').onclick=()=>nav.classList.remove('open');document.addEventListener('keydown',e=>{if(e.key==='Escape')nav.classList.remove('open')});shell.hidden=false;document.querySelector('[data-loading]').hidden=true}
