import{PROTOCOL_VERSION,SCHEMA_VERSION,SyncError}from'./constants.js';import{assertJsonSafe}from'./integrity.js';
const ACTIONS=['health','initialize','sync','pull','push','getManifest'];
export class SyncClient{constructor({endpointUrl,key,fetchImpl=fetch,timeoutMs=15000}){this.endpointUrl=endpointUrl;this.key=key;this.fetch=fetchImpl;this.timeoutMs=timeoutMs}request(action,data={}){if(!ACTIONS.includes(action))throw new TypeError('Action non autorisée');let url;try{url=new URL(this.endpointUrl);if(url.protocol!=='https:')throw 0}catch{throw new SyncError('ENDPOINT_INVALID','Endpoint HTTPS invalide')}assertJsonSafe(data);const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),this.timeoutMs);return this.fetch(url,{method:'POST',headers:{'Content-Type':'text/plain;charset=utf-8'},body:JSON.stringify({action,key:this.key,protocolVersion:PROTOCOL_VERSION,schemaVersion:SCHEMA_VERSION,data}),signal:controller.signal}).then(async response=>{clearTimeout(timer);if(response.status===403)throw new SyncError('AUTH_INVALID','Clé refusée');if(response.status===429)throw new SyncError('QUOTA_EXCEEDED','Quota atteint');if(!response.ok)throw new SyncError('APPS_SCRIPT_ERROR',`Erreur HTTP ${response.status}`);let result;try{result=await response.json()}catch{throw new SyncError('REMOTE_DATA_CORRUPTED','Réponse JSON invalide')}if(!result||typeof result!=='object'||result.ok===false)throw new SyncError(result?.error?.code||'APPS_SCRIPT_ERROR',result?.error?.message||'Réponse serveur invalide');if(result.protocolVersion!==undefined&&result.protocolVersion!==PROTOCOL_VERSION)throw new SyncError('PROTOCOL_MISMATCH','Protocole serveur incompatible');return result}).catch(error=>{clearTimeout(timer);if(error instanceof SyncError)throw error;if(error?.name==='AbortError')throw new SyncError('NETWORK_OFFLINE','Délai réseau dépassé');throw new SyncError('NETWORK_OFFLINE','Réseau indisponible',{cause:error?.message})})}health(){return this.request('health')}initialize(data){return this.request('initialize',data)}sync(data){return this.request('sync',data)}pull(data){return this.request('pull',data)}push(data){return this.request('push',data)}getManifest(){return this.request('getManifest')}}
