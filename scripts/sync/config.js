import{PROTOCOL_VERSION,SCHEMA_VERSION,SyncError}from'./constants.js';
const KEY='quiz-tsi-sync-config-v1';
export function defaultSyncConfig(){return{endpointUrl:'',syncKey:'',verified:false,autoSyncEnabled:false,deviceId:crypto.randomUUID(),protocolVersion:PROTOCOL_VERSION,schemaVersion:SCHEMA_VERSION,lastServerRevision:0,lastSuccessfulSyncAt:null,lastAttemptAt:null}}
export function loadSyncConfig(storage=localStorage){try{const raw=storage.getItem(KEY);return raw?{...defaultSyncConfig(),...JSON.parse(raw)}:defaultSyncConfig()}catch(error){throw new SyncError('LOCAL_STORAGE_ERROR','Configuration de synchronisation illisible',{cause:error.message})}}
export function saveSyncConfig(config,storage=localStorage){const next={...config,protocolVersion:PROTOCOL_VERSION,schemaVersion:SCHEMA_VERSION};try{storage.setItem(KEY,JSON.stringify(next));return next}catch(error){throw new SyncError('LOCAL_STORAGE_ERROR','Configuration de synchronisation non enregistrée',{cause:error.message})}}
export function clearSyncConfig(storage=localStorage){try{storage.removeItem(KEY)}catch(error){throw new SyncError('LOCAL_STORAGE_ERROR','Configuration locale non supprimée',{cause:error.message})}return defaultSyncConfig()}
export function validateSyncCredentials({endpointUrl,syncKey}){let url;try{url=new URL(endpointUrl)}catch{throw new SyncError('ENDPOINT_INVALID','URL invalide')}if(url.protocol!=='https:'||!url.pathname)throw new SyncError('ENDPOINT_INVALID','URL invalide');if(typeof syncKey!=='string'||!syncKey.trim())throw new SyncError('AUTH_INVALID','Clé absente');return{endpointUrl:url.toString(),syncKey:syncKey.trim()}}
