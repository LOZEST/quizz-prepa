const clean=e=>String(e||'').replace(/[\r\n<>]/g,' ').slice(0,300);export const operation=(questionId,type,payload,expectedVersion=null)=>({operationId:globalThis.crypto?.randomUUID?.()||`${Date.now()}-${Math.random()}`,questionId,type,expectedVersion,payload,createdAt:new Date().toISOString(),attempts:0,lastError:null});export function enqueue(cache,op){if(!cache.data.outbox.some(x=>x.operationId===op.operationId)){cache.data.outbox.push(op);cache.save()}return op}export function fail(cache,id,error){const op=cache.data.outbox.find(x=>x.operationId===id);if(op){op.attempts++;op.lastError=clean(error?.message||error);cache.save()}}
